{
  "description": "Feeds the pet: commits, green tests, compactions and closed plan tasks become XP; the session's shape becomes habit counters.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/ccpet\" link",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" ]; then exec \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" hook; fi; exec \"${CLAUDE_PLUGIN_ROOT}/bin/ccpet\" hook",
            "timeout": 5
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" ]; then exec \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" hook; fi; exec \"${CLAUDE_PLUGIN_ROOT}/bin/ccpet\" hook",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" ]; then exec \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" hook; fi; exec \"${CLAUDE_PLUGIN_ROOT}/bin/ccpet\" hook",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" ]; then exec \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/ccpet-run\" hook; fi; exec \"${CLAUDE_PLUGIN_ROOT}/bin/ccpet\" hook",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
